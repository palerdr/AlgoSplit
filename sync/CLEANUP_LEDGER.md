# Cleanup Ledger

Generated by Agent F on branch `perf/f-browser-loop`.
Last updated: 2026-03-12

## Summary

| Category | Safe to Delete | Keep | Needs Review |
|----------|---------------|------|-------------|
| `legacy/` | 3 | 0 | 0 |
| Root files | 2 | 3 | 1 |
| `frontend/` | 0 (whole dir) | 0 | 1 (entire directory) |
| `app/` dead code | 1 (deleted) | 0 | 0 |
| **Total** | **6** | **3** | **2** |

---

## `legacy/` — Old Python prototype (safe to delete)

All three files are standalone Python scripts from an earlier CLI prototype. None are imported by `backend/`, `frontend/`, or `app/`. They reference modules (`movement_patterns`, `exercise_parser`) that don't exist in their directory — they were part of a separate, now-abandoned codebase.

| File | Status | Evidence |
|------|--------|----------|
| `legacy/baseClasses.py` | SAFE TO DELETE | No imports found. Imports `movement_patterns` which doesn't exist in repo. Superseded by `backend/core/MainClasses.py`. |
| `legacy/netweeklystim.py` | SAFE TO DELETE | No imports found. Standalone model file. Superseded by `backend/core/` stimulus engine. |
| `legacy/user_input.py` | SAFE TO DELETE | No imports found. Imports `exercise_parser` and `baseClasses` locally. Interactive CLI tool, fully replaced by API + app. |

---

## Root-level files

| File | Status | Evidence |
|------|--------|----------|
| `my_split.json` | SAFE TO DELETE | Sample data file. Not referenced anywhere in code (grep: 0 hits). |
| `progress.txt` | SAFE TO DELETE | Project tracker from early development. References "Phase 4: Frontend [0% COMPLETE]" — completely stale. Not imported or read by any code. |
| `requirements.txt` | KEEP | Root-level duplicate of `backend/requirements.txt`. May be used by deployment. Verify with deploy config before removing. |
| `pytest.ini` | KEEP | Used by backend test runner. |
| `README.md` | KEEP | Standard repo readme. |
| `design/` | NEEDS REVIEW | Contains 5 design reference images (whoop charts, body mesh mockups). Not imported by code but may be useful as design references. Low priority. |

---

## `frontend/` — Web frontend (migration candidate)

The entire `frontend/` directory is the Vite + React web app that is being migrated to the Expo mobile app (`app/`). It is still a separate active surface per `AGENTS.md` and CI runs its tests.

**Do NOT delete yet.** Document overlap for future cleanup after migration completes.

### Overlap with `app/` (duplicated functionality)

| `frontend/src/` | `app/src/` equivalent | Notes |
|------------------|-----------------------|-------|
| `api/client.ts` | `api/client.ts` | Both exist, mobile version adds SecureStore + platform detection |
| `api/splits.api.ts` | `api/splits.api.ts` | Same API endpoints, different client |
| `api/workouts.api.ts` | `api/workouts.api.ts` | Same endpoints |
| `api/analysis.api.ts` | `api/analysis.api.ts` | Same endpoints |
| `api/comparisons.api.ts` | `api/comparisons.api.ts` | Same endpoints |
| `api/programs.api.ts` | `api/programs.api.ts` | Same endpoints |
| `api/bodyweight.api.ts` | `api/bodyweight.api.ts` | Same endpoints |
| `api/auth.api.ts` | `api/auth.api.ts` | Same endpoints |
| `api/customExercises.api.ts` | `api/customExercises.api.ts` | Same endpoints |
| `api/mesoTemplates.api.ts` | `api/mesoTemplates.api.ts` | Same endpoints |
| `api/periodization.api.ts` | `api/periodization.api.ts` | Same endpoints |
| `api/sessionTemplates.api.ts` | `api/sessionTemplates.api.ts` | Same endpoints |
| `stores/compareStore.ts` | `stores/compareStore.ts` | Both use Zustand |
| `stores/settingsStore.ts` | `stores/settingsStore.ts` | Both use Zustand |

### Unique to `frontend/` (not yet migrated or web-only)

| File/Dir | Notes |
|----------|-------|
| `stores/analysisStore.ts` | Analysis workflow state — not yet in mobile |
| `stores/programStore.ts` | Program builder state — not yet in mobile |
| `stores/splitCreateStore.ts` | Split creation wizard state — not yet in mobile |
| `pages/` (16 page dirs) | Web routing / page components |
| `components/exercises/` | Exercise UI components |
| `components/programs/` | Program UI components |
| `components/layout/` | Web-specific layout shell |
| `features/auth/` | Web auth flow |
| `features/workout/` | Web workout flow |
| `hooks/useIsMobile.ts` | Web-only responsive hook |

**Status: NEEDS REVIEW** — Cannot delete until migration is declared complete and CI no longer tests `frontend/`.

---

## `app/` — Dead code (cleaned)

| File | Status | Evidence | Action |
|------|--------|----------|--------|
| `app/src/components/3d/BodyModel.tsx` | DELETED | grep for `BodyModel` found 0 imports (only self-references in the file itself). Superseded by `InteractiveBody.tsx` which is the active 3D body component. | Deleted in this commit. |

---

## Notes

- The `backend/core/exercise_parser.py` line 511 contains the string "legacy mapping" but this is a code comment, not an import of the `legacy/` directory.
- The `expo-file-system/legacy` import in `InteractiveBody.tsx` is the Expo SDK module path, unrelated to the `legacy/` directory.
- Root `requirements.txt` is identical in purpose to `backend/requirements.txt` — may be a deploy artifact. Verify before removing.
