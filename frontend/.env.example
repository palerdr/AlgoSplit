# API URL - set this to your backend URL in production
# For local development, the Vite proxy handles this
VITE_API_URL=https://your-backend-api.com

# Optional: override if backend uses a custom CSRF cookie name
VITE_CSRF_COOKIE_NAME=splitai_csrf_token
