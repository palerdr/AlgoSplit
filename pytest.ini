[pytest]
testpaths = backend/tests
addopts = -q --strict-markers --disable-warnings
norecursedirs = .git .venv app frontend node_modules __pycache__ .pytest_cache
markers =
    integration: End-to-end API and route-level integration tests
