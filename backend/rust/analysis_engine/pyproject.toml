[build-system]
requires = ["maturin>=1.8,<2"]
build-backend = "maturin"

[project]
name = "analysis-engine-rs"
version = "0.1.0"
requires-python = ">=3.10"

[tool.maturin]
module-name = "analysis_engine_rs"
features = ["pyo3/extension-module"]
